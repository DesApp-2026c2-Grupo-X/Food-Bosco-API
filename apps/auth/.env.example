# Auth Service — variables de entorno (desarrollo)
# Copiar a .env y completar (el .env no se versiona).

PORT=4201
NODE_ENV=development

# Conexión a MongoDB (la provee el usuario)
# Nube (Atlas):
#   MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/fastfood
# Local:
MONGODB_URI=mongodb://localhost:27017/fastfood

# JWT (secreto compartido con el gateway)
JWT_SECRET=dev-secret-change-me
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
PASSWORD_RECOVERY_EXPIRES_IN=1h
# Intervalo mínimo entre solicitudes de recuperación del mismo usuario (anti-abuso)
PASSWORD_RECOVERY_MIN_INTERVAL=60s

# Envío de correo de recuperación
# EMAIL_PROVIDER=log  → no envía correos reales, solo registra el envío (desarrollo)
# EMAIL_PROVIDER=resend → requiere RESEND_API_KEY
EMAIL_PROVIDER=log
EMAIL_FROM=Food Bosco <no-reply@foodbosco.local>
RESEND_API_KEY=
# URL base de cada frontend que recibe el enlace de restablecimiento (según el rol del usuario)
STORE_URL=http://localhost:5173
ADMIN_URL=http://localhost:5174
BRANCH_URL=http://localhost:5175
RIDER_URL=http://localhost:5176
PASSWORD_RESET_PATH=/reset-password

# Acceso "interno" (gateway) a GET /v1/users/{id}
INTERNAL_API_TOKEN=dev-internal-token

# Validación de sucursal contra Commerce Service (RQ-AUTH-13, pendiente)
COMMERCE_SERVICE_URL=http://localhost:4202

# Seed de usuarios (npm run seed)
# Los datos de los usuarios (email, nombre, rol, teléfono, vehículo) viven en
# src/seed/data/auth.json (con override por entorno: auth.<NODE_ENV>.json).
# Acá solo se configuran las contraseñas.
SEED_SUPER_ADMIN_PASSWORD=Admin123!
SEED_CUSTOMER_PASSWORD=Cliente123!
SEED_BRANCH_ADMIN_PASSWORD=Sucursal123!
SEED_RIDER_PASSWORD=Repartidor123!
